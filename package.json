{
  "name": "template-repo",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --stream build",
    "dev": "pnpm -r --stream --parallel dev",
    "format": "prettier --write .",
    "test": "pnpm -r --stream --parallel test",
    "clean": "pnpm -r --stream --parallel clean && rm -rf node_modules"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{js,ts,tsx,css,json,md}": "prettier --write"
  },
  "prettier": {
    "semi": false,
    "printWidth": 100
  },
  "packageManager": "pnpm@10.12.1"
}
